{
    "name": "matlance-root",
    "private": true,
    "scripts": {
        "client": "npm --prefix client run dev",
        "server": "npm --prefix server run dev",
        "install-all": "npm install && npm --prefix client install && npm --prefix server install",
        "dev": "cross-env NODE_OPTIONS=--no-deprecation concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
        "deploy:vps": "bash deploy/vps/deploy.sh",
        "deploy:vps:bootstrap": "bash deploy/vps/bootstrap.sh"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "cross-env": "^7.0.3"
    },
    "dependencies": {
        "express-mongo-sanitize": "^2.2.0",
        "express-rate-limit": "^8.2.1"
    }
}
